# Extra dependencies for the evaluation pipeline (scripts/ + eval/).
# Install with: pip install -r requirements-eval.txt
#
# Local (no GPU needed):
arxiv>=2.0.0           # download_arxiv.py
pandas>=2.0           # eval/review_candidates.ipynb
ipywidgets>=8.0       # eval/review_candidates.ipynb (interactive review UI)
# tqdm, PyMuPDF, openai, python-dotenv already in requirements.txt

# Cloud / GPU host (Kaggle, RunPod) — install only there:
# transformers>=4.45
# accelerate>=0.34
# peft>=0.13
# bitsandbytes>=0.43
# torch>=2.3
# unsloth                 # 2x faster QLoRA, recommended
# lm-eval[hf]>=0.4.4      # for run_baselines.py
